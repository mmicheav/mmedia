{% extends 'base.twig' %}

{% block title %}{{ articles.title }}{% endblock %}

{% block content %}
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h2 class="card-title text-center">{{ articles.title }}</h2>
                        <p class="card-text">{{ articles.content }}</p>
                        <small class="text-muted">Por: <b>{{ articles.username }}</b> - {{ articles.created_at}}</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
